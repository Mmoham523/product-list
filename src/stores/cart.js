// Pinia store for cart (alternative to composables)
import { defineStore } from 'pinia'

export const useCartStore = defineStore('cart', {
  state: () => ({
    // cart state
  }),
  actions: {
    // cart actions
  }
})

